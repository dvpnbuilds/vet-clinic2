{
  "name": "vetflow",
  "version": "0.1.0",
  "private": true,
  "description": "Booking and automation demo for veterinary clinics",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "seed": "node db/seed.js",
    "test": "node --test"
  },
  "dependencies": {
    "@libsql/client": "^0.15.15",
    "express": "^5.1.0"
  }
}
