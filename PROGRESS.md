# Progress

Current phase: 11 (audited PASS; v2 complete)

Phase 11 scope: add clinic-scoped invoice and invoice-item records created from the appointment service catalog, with a single-purpose public receipt token, staff-only paid/unpaid controls, a printable profile-branded receipt, and dashboard revenue/outstanding records. The UI must clearly present these as internal payment records rather than a payment gateway, and tests must reconcile dashboard totals to persisted invoices.

Phase 10 scope: deepen the staff dashboard with clinic-scoped, SQL-derived analytics for no-show trends, busiest appointment times and services, reminder-to-confirmation conversion, and vaccine-campaign recovery. Every metric must use persisted appointment/reminder data, retain profile-owned presentation copy, and have test coverage that reconciles each displayed figure to an explicit SQL/manual count.

Independent re-audit: 2026-07-24 — PASS / GO. Phase 6 renders live seeded calendar, pending-confirmation, vaccine-due, and reminder-attributed recovered-revenue data; dashboard records are rendered as text nodes; staff-only APIs require STAFF_PASSCODE; both clinic profiles keep their own branding and language. Post-audit remediation was rechecked: durable idempotent booking retries, atomic booking/block-off exclusion, Vercel-safe retry IDs, clinic-scoped reminder work, local-time vaccine due windows, explicit POST owner actions, and conservative reminder reconciliation all have automated coverage. Verification: npm.cmd test (40/40), node --check on every JS file, git diff --check, and npm.cmd audit --omit=dev --audit-level=high (0 vulnerabilities). No P1/P2 blockers found.

Post-audit corrective remediation: 2026-07-24 — Booking requests now require an Idempotency-Key. A clinic-scoped durable record binds that key to a fingerprint and persisted booking result, so safe retries replay the original appointment rather than create new owners, pets, or reservations. The browser funnel generates and reuses a key for a submission; chat uses its tool-call ID. The Vercel reminder retry handler now reads its dynamic ID from either Express params or Vercel query. Local verification pending.

Additional P2 remediation: 2026-07-24 — Vaccine reminder due times are now resolved as 9:00 AM in the clinic’s IANA timezone, not UTC. Demo access accepts only the request header, never a URL query parameter. Reminder claims and context reads now carry the active clinic boundary as defense in depth. Local verification: npm test (38 passing), JavaScript syntax checks, and git diff --check.

Post-audit reliability remediation: 2026-07-24 — Block-off insertion is now an atomic conditional write that cannot win alongside an overlapping pending/confirmed booking; a concurrent regression test proves exactly one operation succeeds. Provider calls now have a deadline, delivery-attempt history is retained, failures emit an operational reconciliation alert, and staff-only reconciliation/retry endpoints are available. Local verification: npm test (36 passing), JavaScript syntax checks, and git diff --check. Booking idempotency remains outstanding; it was not enabled after the local libSQL driver rejected the proposed transactional reservation write at commit time.

Post-audit P2 remediation: 2026-07-24 — IANA local-time conversion now skips DST spring gaps and selects the first occurrence during autumn overlaps, preventing duplicate persisted slots. Vaccine enqueueing and due-reminder delivery are scoped to the active clinic. Local verification: npm test (35 passing), JavaScript syntax checks, and git diff --check.

Post-audit P1 remediation: 2026-07-24 — Past dates and elapsed same-day slots are rejected before slot generation, and every booking transaction rechecks that the chosen slot is still in the future. Appointment reminder scheduling now skips 24-hour and 2-hour reminders whose windows have already passed. Owner action links render a read-only confirmation page on GET; confirmation and rescheduling state changes require an explicit POST with the bearer token. Local verification: npm test (33 passing), JavaScript syntax checks, and git diff --check.

Final audit: 2026-07-24 — NO-GO. The independent source audit found P1 issues in staff authorization, dashboard stored XSS, and reminder delivery recovery, plus P2 validation, rate-limit, and profile-isolation issues. The P1/P2 remediation is now implemented locally and 30 automated tests pass, but a fresh independent audit is required before release.

P1 remediation: 2026-07-24 — Staff-only APIs now require a distinct STAFF_PASSCODE; dashboard records render with text nodes; and reminder delivery attempts are persisted before provider calls so ambiguous outcomes are held for reconciliation rather than resent automatically.

P2 remediation: 2026-07-24 — Booking, slot, calendar, and block-off inputs are validated server-side with safe 400 responses; rate limits prefer Vercel's verified client-IP header and never select a forwarded-chain entry; and seed fixtures are profile-owned and excluded from public profile responses. Local verification: npm test (30 passing), JavaScript syntax checks, git diff --check, and npm audit --omit=dev --audit-level=high.

Phase 1 delivery: built on 2026-07-23; audit deferred by user instruction until all planned phases are complete. Local verification: npm test (3 passing) and npm run seed against a temporary libSQL database.

Phase 2 delivery: built on 2026-07-23; audit deferred by user instruction until all planned phases are complete. Implemented service-specific, time-zone-aware slots from persisted vet hours and capacity; durable 10-minute reservation slices and write transactions for double-book prevention; block-offs; and protected slot, booking, block-off, and day/week calendar APIs. Local verification: npm test (6 passing), including concurrent booking attempts.

Phase 3 delivery: built on 2026-07-23; audit deferred by user instruction until all planned phases are complete. Added a mobile-first, clinic-profile-driven funnel with demo-code entry, services, live slots, owner/pet details, conflict recovery, and a thank-you state. It writes through the Phase 2 booking API. Local verification: node syntax checks and npm test (7 passing), including HTTP booking-to-calendar flow. Browser visual QA is deferred to deployment because the available browser surface blocks this local URL.

Phase 4 delivery: built on 2026-07-23; audit deferred by user instruction until all planned phases are complete. Added a DRY_RUN-default notification module for Semaphore and Resend, durable due-and-unsent reminder processing with atomic claims and retry recovery, Taglish/English profile templates, vaccine and no-show campaigns, public single-purpose confirm/reschedule links, an unconfirmed appointment read API, and n8n trigger documentation. Local verification: npm test (12 passing), including repeated cron calls and HTTP cron authorization.

Phase 5 delivery: built on 2026-07-23; audit deferred by user instruction until all planned phases are complete. Added the single OpenRouter gateway, profile-grounded prompts and FAQs, controlled slot lookup/booking tools that use the Phase 2 engine, and a mobile Messenger-style chat UI. Local verification: npm test (15 passing), covering Taglish, FAQ prompt grounding, a real chat booking, and duplicate-booking refusal.

Phase 6 delivery: built on 2026-07-23; audit deferred by user instruction until all planned phases are complete. Added a profile-driven staff dashboard with today’s calendar, unconfirmed visits, vaccines due, and reminder-attributed recovered revenue. Demo-code entry is provided for the dashboard alongside the existing public surfaces. Local verification: npm test (18 passing), including dashboard data, recovered revenue, and Western English no-Taglish checks.

## Phase 1: Foundation — pending
Notes: 2026-07-23 — Initial setup in progress: Node/Express, Turso client/schema/seed, PH and Western profiles, demo access guard, rate limiting, and protected health route. Added Express and @libsql/client; installation and verification remain.
Audit: 2026-07-23 — FAIL. Local seed, schema coverage, authenticated health route, profiles, and two access tests pass. Before Phase 1 can be marked built: replace the instance-local rate-limit Map with durable/shared protection suitable for Vercel; validate Vercel routing with vercel dev; add automated coverage for rate limiting and seed/schema output.

## Phase 2: Scheduling engine — pending
Notes:
Audit:

## Phase 3: Booking funnel — pending
Notes:
Audit:

## Phase 4: Reminder automation — pending
Notes:
Audit:

## Phase 5: AI receptionist — pending
Notes:
Audit:

## Phase 6: Dashboard + polish — audited PASS
Notes: 2026-07-23 — Profile-driven staff dashboard delivers today’s calendar, pending confirmations, vaccine-due follow-up, and reminder-attributed recovered revenue. Staff access uses the distinct STAFF_PASSCODE and all dynamic dashboard values render through text nodes.
Audit: 2026-07-24 — PASS. Completion criteria and post-audit remediation verified independently; no blockers to Phase 7. Evidence: 40 automated tests, JavaScript syntax checks, clean diff check, and high-severity production dependency audit.

## Phase 7: QR walk-in intake — built
Notes: 2026-07-24 — Added a printable, local-SVG QR for `/w`; the profile-branded mobile form creates clinic-scoped owner, pet, confirmed walk-in appointment, and walk-in records with a captured mobile number. The protected dashboard now shows today’s walk-in queue. The public write endpoint has a durable per-IP rate limit; no third-party QR service or dependency was introduced. Local verification: npm.cmd test (44 passing), syntax checks for new JavaScript, and git diff --check. Browser visual QA was attempted but the in-app browser blocks localhost navigation.
Audit: 2026-07-24 — PASS / GO. Re-audit verified the P1 production path is fixed: `api/walk-ins.js` now applies the durable public-write limiter before the shared walk-in route, matching existing Vercel wrappers. The scanned form and QR print page now render the active profile name, title, language, and copy with no hardcoded clinic or language fallback strings. Completion criteria pass: clinic-scoped transactional owner/pet/confirmed-appointment/walk-in persistence captures a phone number; the staff-only dashboard renders the clinic-scoped local-day queue through text nodes; public input is validated and rate-limited durably/fail-closed; and the local SVG QR plus printable page point to `/w`. Automated coverage includes the Vercel handler export, active-profile render hooks, over-limit no-write behavior, malformed input, persistence, queue inclusion, and deterministic QR matrix/SVG/length checks. Local verification: npm.cmd test (46/46), JavaScript syntax checks, git diff --check, and npm.cmd audit --omit=dev --audit-level=high (0 vulnerabilities). Non-blocking: no native scanner is available locally, so the QR test is deterministic encoder coverage rather than a camera scan; handler/profile tests are source/composition coverage, not browser/Vercel live QA.

## Phase 8: Post-visit review requests — built
Notes: 2026-07-24 — Added clinic-scoped `reviews` records, staff completion endpoint, delayed review delivery through the existing cron + notify() path, and single-use tokenized rating/feedback endpoints. High ratings route to profile-owned Google review URLs; low ratings persist private feedback visible to staff. No dependencies added. Local verification: npm.cmd test (51 passing), new JavaScript syntax checks, and git diff --check.
Audit: 2026-07-24 — PASS / GO. Re-audit verifies the recovery blocker is fixed: a failure before `delivery_started_at` is persisted releases the clinic-scoped claim back to `pending`, so the next due-and-unsent cron tick can safely retry; after delivery begins, failures remain held as `failed` to prevent duplicate SMS/email. Regression coverage injects a pre-delivery template failure and verifies a later cron call delivers the same review exactly once. Completion remains transactionally idempotent (one review per appointment); delivery uses `notify()` only; Express/Vercel paths, profile-scoped Google gate URLs, token/rating/feedback validation, dashboard text-node rendering, and schema/seed/index integrity pass. Verification: npm.cmd test (52/52), Phase 8 syntax checks, and git diff --check. No blockers to Phase 9.

## Phase 9: Owner pet card — built
Notes: 2026-07-24 — Added a read-only, profile-branded mobile pet passport with a separately persisted expiring per-pet UUID token. It displays pet profile, complete vaccination history, next due vaccination, and prior visits; staff can regenerate/open a card link from the calendar, immediately revoking the prior token. Local verification: npm.cmd test (57 passing), new JavaScript syntax checks, and git diff --check. No dependencies added.
Audit: 2026-07-24 — PASS / GO. Re-audit confirms the corrective remediation resolves every P2: public invalid, expired, and unexpected card-read errors now use the active profile’s `petCard.unavailable` copy; past visits are restricted to completed elapsed appointments; and the public payload is minimized to card-rendered pet, vaccine, and visit fields only. Next-due selection uses the latest administration record for each vaccine and then selects the nearest current/upcoming due date. Token generation/revocation, expiry, active-clinic predicates, staff-only issuance, Express/Vercel surfaces, schema/seed integrity, and dashboard `noopener` handling remain correct. Regression coverage proves PH error copy, minimal payload fields, non-visit exclusion, and superseded-vaccine selection. Verification: npm.cmd test (58/58), targeted syntax checks, git diff --check, and npm.cmd audit --omit=dev --audit-level=high (0 vulnerabilities). No blockers to Phase 10.

## Phase 10: Analytics deepening — built
Notes: 2026-07-24 — Added clinic-scoped dashboard signals for seven-day no-show rate/trend, busiest local appointment time and service over the latest 30 days, sent confirmation-reminder conversion, and sent vaccine-campaign recovery. Seed data now contains profile-owned historical records so every metric renders meaningfully in the demo without altering primary pet-card/reminder flows. The responsive analytics section uses profile-owned PH/Western copy and safe text-node rendering. `test/dashboard.test.js` reconciles every figure to explicit SQL/manual counts. Local verification: npm.cmd test (59/59), targeted JavaScript syntax checks, and git diff --check. Independent audit pending.
Audit: 2026-07-24 — FAIL / NO-GO. Independent Phase 10 audit found no P1 defects and confirmed that the dashboard uses persisted appointment/reminder data, profile-owned labels, text-node rendering, empty states, and IANA-aware runtime local-hour conversion. However, the phase does not yet meet its approved verification and isolation bar: (1) the manual reconciliation test asserts only seven-day no-show totals, not each rendered daily trend value or the computed overall/daily rate; (2) its busiest-hour SQL hardcodes `+08:00` and runs only against the PH fixture, so it does not independently reconcile the Western clinic's local-time metric; and (3) reminder-conversion joins `appointments` only by ID while filtering the reminder clinic, although the schema has no composite constraint requiring those clinic IDs to agree. Add an appointment clinic predicate to that metric and targeted, independent PH/Western SQL reconciliation tests (including zero-data and per-day/rate assertions), then request re-audit. Verification performed: `npm.cmd test` (59/59), targeted JavaScript syntax checks, `git diff --check`, and `npm.cmd audit --omit=dev --audit-level=high` (0 vulnerabilities).
Re-audit: 2026-07-24 — PASS / GO. The three P2 findings are resolved: reminder conversion now requires the joined appointment to carry the same clinic ID; the PH test independently reconciles all seven local calendar-day trend rows, their rates, and the aggregate rate against SQL; and the Western seeded profile independently computes its busiest local hour through `Intl` with `Australia/Sydney`, never a fixed offset. All four metrics remain derived from persisted, profile-scoped data with meaningful profile-owned seed history and safe text-node rendering; empty states retain null-rate handling. Verification: `npm.cmd test` (60/60), targeted JavaScript syntax checks, and `git diff --check`. No P1/P2 blockers remain; Phase 11 is unblocked.

## Phase 11: Invoicing & records — built
Notes: 2026-07-24 — Added clinic-scoped invoices and immutable itemized service lines for completed appointments, staff-only paid/unpaid status updates, UUID-tokenized read-only printable receipts, and dashboard paid/outstanding record totals. Receipt and dashboard copy are profile-owned and explicitly frame invoices as records, not payment collection. `test/invoices.test.js` proves correct catalog pricing, idempotent generation, status persistence, receipt access, and SQL reconciliation of dashboard totals. Local verification: npm.cmd test (62/62), syntax checks, and git diff --check. Independent audit pending.
Audit: 2026-07-24 — FAIL / NO-GO. The core completion flow, schema constraints, clinic predicates, immutable catalog-priced service line, staff protection on Express/Vercel mutation routes, UUID receipt token, minimized public receipt payload, record-only UI wording, and invoice-sum dashboard SQL are present. However, Phase 11 is not safely idempotent under concurrent generation: two simultaneous `generateInvoice()` calls for the same completed, uninvoiced appointment yield one success and one `SQLITE_BUSY: database is locked` rejection rather than two successful replays of the same invoice. The implementation must make concurrent creation/retry safe (then cover it with a regression test). In addition, the public receipt route returns the hardcoded English `This receipt is unavailable.` for invalid/expired/unexpected errors instead of `profile.invoices.unavailable`, violating the profile-owned public-copy rule and breaking PH localization. Update that route and add PH/Western public-error coverage. The existing invoice tests also exercise DB helpers rather than the staff/public HTTP paths, so extend coverage to the protected Express/Vercel handlers while remediating. Verification performed: npm.cmd test (62/62), targeted JavaScript syntax checks, git diff --check, npm.cmd audit --omit=dev --audit-level=high (0 vulnerabilities), and a direct concurrent-generation probe showing one fulfilled invoice creation plus one `SQLITE_BUSY` rejection. No P1 defects were found; these P2 correctness/profile issues block Phase 12.
Re-audit: 2026-07-24 — PASS / GO. The P2 remediations are verified: concurrent same-process generation is coalesced by appointment, and bounded retry on transient SQLite lock/busy errors replays the durable invoice record rather than surfacing a failed duplicate action. The new regression covers four simultaneous generation requests returning one invoice ID. Public receipt errors now resolve through `getActiveClinicProfile().invoices.unavailable`; an independent handler probe confirmed the PH response is `Hindi available ang receipt na ito.` and the Western response remains English. The source confirms clinic-scoped invoice/receipt queries, persisted service-line snapshots, invoice-status persistence, staff-only Express and Vercel mutation wrappers, UUID single-purpose receipt reads, minimal public receipt fields, record-only printable UI, and dashboard paid/outstanding sums. Verification: npm.cmd test (63/63), targeted JavaScript syntax checks, git diff --check, and npm.cmd audit --omit=dev --audit-level=high (0 vulnerabilities). No P1/P2 blockers remain; all approved v2 phases are audited PASS.

## Decision log
- 2026-07-24: Phase 9 uses one active expiring token per pet. Staff regeneration atomically replaces the token, invalidating the previous public URL immediately; public card reads return only the token-selected pet under the active clinic boundary and never create owner accounts.
- 2026-07-24: Phase 8 records review delivery state separately from reminders so review requests retain their own one-per-appointment token, rating, and private feedback while still using the existing cron trigger and notify() transport. A review is claimed and marked delivery-started before contacting a provider, preventing duplicate post-visit messages after ambiguous delivery outcomes.
- 2026-07-24: Phase 7 uses an in-app QR encoder rather than a third-party QR host or dependency, keeping the printed walk-in flow self-contained. Intake creates a confirmed appointment without a slot reservation because it records arrival, not a scheduled time; the record remains clinic-scoped and is visible only through the staff dashboard.
- 2026-07-24: P2 remediation adds strict boundary validation, Vercel-safe IP selection, and profile-owned seed fixtures; public profile payloads exclude all server-only seed data.
- 2026-07-24: P1 remediation separates public DEMO_PASSCODE access from staff-only STAFF_PASSCODE access; it does not create pet-owner accounts or in-memory sessions.
- 2026-07-24: Reminder delivery is conservatively at-most-once after a provider call begins. Ambiguous provider/database outcomes are retained for reconciliation instead of retried automatically, preventing duplicate SMS/email charges.
- 2026-07-23: Final audit conditional pass after moving pre-access copy into clinic profiles and replacing Express-only request header calls with Vercel-safe helpers.
- 2026-07-23: Recovered revenue is an attribution metric: the sum of confirmed appointments with at least one sent appointment reminder, not a claim of payment collection.
- 2026-07-23: Phase 6 was built without an audit at user direction; audit is deferred until all planned phases are complete.
- 2026-07-23: AI tool calls are limited to find_slots and book_appointment, both implemented through the existing scheduling engine; the model never writes directly to the database.
- 2026-07-23: Phase 5 was built without an audit at user direction; audit is deferred until all planned phases are complete.
- 2026-07-23: Reminder records use durable dedupe keys plus short-lived processing claims so late or repeated cron ticks self-heal without duplicate delivery.
- 2026-07-23: Phase 4 was built without an audit at user direction; audit is deferred until all planned phases are complete.
- 2026-07-23: Added profile-owned booking copy for PH Taglish and Western English so public funnel language, branding, services, and labels are never hardcoded in the frontend.
- 2026-07-23: Phase 3 was built without an audit at user direction; audit is deferred until all planned phases are complete.
- 2026-07-23: Phase 2 uses durable 10-minute per-vet capacity reservations with a unique database constraint, allowing one booking transaction to win and rejecting concurrent overlap attempts safely.
- 2026-07-23: Phase 2 was built without an audit at user direction; audit is deferred until all planned phases are complete.
- 2026-07-23: User directed that phase audits occur after all phases, overriding the normal audit gate between phases.
- 2026-07-23: Rate limiting now uses a Turso-backed atomic counter rather than instance memory, so the public-demo protection works across Vercel instances.
- 2026-07-23: Phase 1 audit failed pending durable Vercel-safe rate limiting, Vercel dev validation, and broader foundation test coverage.
- 2026-07-23: Added Express 5 and @libsql/client as the Phase 1 runtime dependencies.
- 2026-07-23: Standalone product, not an evolution of the old vet-clinic-assistant demo.
- 2026-07-23: Deploy on Vercel (Hobby). Rules out file-SQLite + node-cron.
- 2026-07-23: Database = Turso (libSQL) — free, no pausing, SQLite-compatible. Chosen over Supabase to avoid Postgres migration and the 7-day pause.
- 2026-07-23: Cron = n8n Schedule node on DV's VPS, POST to /api/cron/reminders. Trigger only; reminder logic stays in the app. GitHub Actions is the fallback trigger.
- 2026-07-23: Reminders designed as "windows not stopwatches" — cron route sends due-and-unsent, so late/skipped ticks self-heal.
- 2026-07-23: v1 spice = vaccine reminders, no-show reducer, AI receptionist, ₱-recovered metric. QR intake, review requests, pet card moved to v2 (see V2.md).
- 2026-07-23: Channels for v1 = SMS (Semaphore) + email (Resend). Viber/Messenger deferred.
